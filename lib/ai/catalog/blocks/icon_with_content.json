{
    "id": "icon_with_content",
    "label": "Icon With Content",
    "purpose": "One icon + heading + text item inside a horizontal ticker",
    "category": "trust-badges",
    "tags": [],
    "settings": {
        "title": "text",
        "text": "richtext"
    },
    "_notes": "'text' is richtext and MUST be wrapped in <p> tags. The block's icon, icon position, and layout are fixed by the base theme's seed (this block only ever appears inside horizontal-ticker's fixed_blocks list) — write only 'title'/'text', never an 'icon'. Distinct from icon-with-content (hyphenated) used elsewhere — this one is horizontal-ticker's own version."
}
